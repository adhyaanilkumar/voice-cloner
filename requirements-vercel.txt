# Minimal requirements for Vercel deployment
# Note: Heavy ML libraries (torch, tensorflow) are not included
# as they exceed Vercel's size limits. The core functionality
# uses ElevenLabs API which doesn't require these.

# Web framework
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# Database (optional for serverless)
sqlalchemy>=2.0.0

# Data handling (minimal)
numpy>=1.21.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# API Services
requests>=2.31.0

